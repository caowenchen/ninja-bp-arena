-- ============================================================================
-- v0.4.0 建房 RPC 签名收敛
--
-- 0003 新增了带默认值的第六个参数，但 PostgreSQL 会保留 0001 的五参数重载，
-- 导致五参数调用无法判断该选择哪个函数。删除旧重载后，旧调用会自然匹配
-- 六参数函数的默认值，因此仍保持向后兼容。
-- ============================================================================

drop function if exists public.create_room_transaction(
  uuid,
  text,
  text,
  jsonb,
  jsonb
);
