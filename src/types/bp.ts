/**
 * BP 类型（Shared BP Core 单一来源，见 ../../shared/bp-core/types.ts）。
 */
export type {
  Side,
  BPActionType,
  MatchStatus,
  GameStatus,
  BPSequenceStep,
  BattleRule,
  BPAction,
  MatchTimerState,
} from '../../shared/bp-core/types'
export { SIDE_TEXT, ACTION_TEXT } from '../../shared/bp-core/types'
