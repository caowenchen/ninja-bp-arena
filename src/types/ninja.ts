/**
 * 忍者类型（Shared BP Core 单一来源，见 ../../shared/bp-core/types.ts）。
 * 组件仍可从 '@/types/ninja' 导入，保持路径稳定。
 */
export type { Ninja, NinjaQuality } from '../../supabase/functions/_shared/bp-core/types'
export { NINJA_QUALITIES } from '../../supabase/functions/_shared/bp-core/types'
